# diesel_dart

![Dart](https://img.shields.io/badge/Dart-%3E%3D3.5-0175C2?logo=dart&logoColor=white)
![Backends](https://img.shields.io/badge/backends-SQLite%20%7C%20Postgres-informational)
![Status](https://img.shields.io/badge/status-experimental-orange)
![Style](https://img.shields.io/badge/style-lints-brightgreen)
![License](https://img.shields.io/badge/license-TBD-lightgrey)

**A type-safe, migration-first ORM for Dart — a compile-time-checked query builder, a
diesel-compatible migration CLI, code-generated row mappers, and a DevTools inspector.**
Inspired by [diesel-rs](https://github.com/diesel-rs/diesel).

- 🛡️ **Type-safe query builder** — columns and predicates carry a phantom table type, so mixing tables in one `WHERE` is a *compile error*, not a runtime surprise.
- 🔌 **Pluggable backends** — one `Connection` interface; **SQLite** and **Postgres** run the *same* DSL, schema, and migrations unchanged.
- 🧬 **Codegen derives** — `@Queryable` / `@Insertable` / `@AsChangeset` / `@Relation` generate readers, `INSERT`/`UPDATE` builders, and self-mapping join queries via `build_runner`.
- 🚚 **Migration-first CLI** — `diesel_dart migration generate/run/revert/redo`, `database reset`, `print-schema`; the migrations dir + tracking table are **interchangeable with the Rust `diesel` CLI** on SQLite.
- 🔎 **DevTools inspector** — a "diesel" tab to browse, filter, sort, and edit tables and run raw SQL against a live connection.
- 🧩 **Build-vs-execute separation** — the query builder is a pure AST → `(sql, params)` transform, so serialization is trivially unit-testable and new backends are drop-in.

```dart
import 'package:diesel/diesel.dart';
import 'package:diesel_sqlite/diesel_sqlite.dart';

import 'schema.dart'; // generated by `diesel_dart print-schema`
import 'models.dart'; // your @Queryable/@Insertable classes + generated mappers

Future<void> main() async {
  final db = SqliteConnection.open('app.db');

  // Write — toInsert() is generated from @Insertable.
  await db.execute(const User(1, 'Bob', 30, 1).toInsert());

  // Read — mixing another table into this WHERE would not compile.
  final adults = await db.fetch(
    from(Users.table)
        .where((Users.age > 18) & Users.name.like('A%'))
        .orderBy(Users.name.asc())
        .map(userMapper.read),
  );
  print(adults);

  await db.close();
}
```

> **Status: early / experimental.** APIs may change; not yet published to pub.dev. Inspired by diesel-rs —
> not affiliated and not a 1:1 port. The diesel-rs alignment roadmap **M1–M5 is complete** (see
> [Roadmap](#roadmap)).

---

## Table of contents

- [Why diesel_dart](#why-diesel_dart)
- [Packages](#packages)
- [Install](#install)
- [Getting started](#getting-started)
- [The query DSL](#the-query-dsl)
- [Codegen (derives)](#codegen-derives)
- [Migrations CLI](#migrations-cli)
- [Backends](#backends)
- [DevTools inspector](#devtools-inspector)
- [diesel-rs alignment](#diesel-rs-alignment)
- [Documentation](#documentation)
- [Roadmap](#roadmap)
- [Contributing](#contributing)
- [License](#license)

## Why diesel_dart

Most Dart data-access options make you choose between *ergonomics* and *safety*. diesel_dart follows
diesel-rs's answer: your **database schema is the source of truth**, a typed schema is generated from it,
and every query is checked against that schema at compile time.

- **The schema is generated, then trusted.** You write SQL migrations, apply them, and run `print-schema`
  to emit a typed `schema.dart`. Columns become `static const` objects (`Users.age` is a
  `ValueColumn<int, Users>`) reused by both the query builder and the codegen annotations.
- **Queries can't reference the wrong table.** `Expression<T, Tbl>` carries a phantom `Tbl` scope, so
  `from(Users.table).where(Posts.title.eq('x'))` is a compile error. After a join the scope relaxes and the
  serializer validates every referenced table is in the `FROM`/`JOIN` clause.
- **Build vs execute.** `QueryBuilder` + `SqlDialect` turn a typed AST into `(String sql, List<Object?>
  params)` with *zero* driver dependency; a `Connection` runs it. That is what keeps serialization
  unit-testable and makes SQLite and Postgres behave identically.

## Packages

This repo is a Dart **pub workspace** (monorepo). Dart SDK: `>=3.5.0 <4.0.0`.

| Package | Role |
|---|---|
| [`diesel`](packages/diesel) | Dialect-agnostic **core**: types, schema, expressions, query/write builders, serializer, `Connection`/`SqlDialect`, annotations. Also ships the **DevTools inspector** (`package:diesel/devtools.dart`). |
| [`diesel_sqlite`](packages/diesel_sqlite) | **SQLite** backend — `SqliteConnection` + `SqliteDialect` (on [`sqlite3`](https://pub.dev/packages/sqlite3)). |
| [`diesel_postgres`](packages/diesel_postgres) | **Postgres** backend — `PostgresConnection` + `PostgresDialect` (on [`postgres`](https://pub.dev/packages/postgres)). |
| [`diesel_cli`](packages/diesel_cli) | The **`diesel_dart`** executable: migrations + `print-schema`. |
| [`diesel_codegen`](packages/diesel_codegen) | **`build_runner`** derives for the annotations. |
| [`diesel_devtools_extension`](packages/diesel_devtools_extension) | Flutter web UI for the DevTools "diesel" tab. |
| [`example/`](example) | End-to-end demo (migrations → schema → models → queries). |

## Install

The packages are **unpublished**, so depend on them by `path:` (inside this repo) or `git:`:

```yaml
dependencies:
  diesel:
    git: { url: https://github.com/you/diesel_dart, path: packages/diesel }
  diesel_sqlite:
    git: { url: https://github.com/you/diesel_dart, path: packages/diesel_sqlite }

dev_dependencies:
  diesel_cli:      # the `diesel_dart` CLI
    git: { url: https://github.com/you/diesel_dart, path: packages/diesel_cli }
  diesel_codegen:  # build_runner derives
    git: { url: https://github.com/you/diesel_dart, path: packages/diesel_codegen }
  build_runner: ^2.4.0
```

## Getting started

The workflow mirrors diesel-rs: **migrate → generate schema → annotate → generate mappers → query.**

```sh
# 1. Configure the CLI (diesel.yaml in your project root):
#      database_url: app.db
#      migrations_dir: migrations

# 2. Scaffold a migration, then edit up.sql / down.sql.
dart run diesel_cli:diesel_dart migration generate create_users

# 3. Apply pending migrations (creates app.db).
dart run diesel_cli:diesel_dart migration run

# 4. Generate the typed schema from the migrated database.
dart run diesel_cli:diesel_dart print-schema -o lib/schema.dart

# 5. Write @Queryable/@Insertable data classes, then generate their mappers.
dart run build_runner build
```

The full walkthrough is in **[docs/getting-started.md](docs/getting-started.md)**.

## The query DSL

Everything below builds a typed `MappedQuery` you run with `db.fetch(...)` (or the diesel-style
`.load(db)` / `.first(db)` / `.optional(db)` terminals). Full reference: **[docs/query-dsl.md](docs/query-dsl.md)**.

```dart
// Predicates: eq/ne/gt/ge/lt/le, isIn/eqAny, between, like, isNull/isNotNull,
// combined with & / | (chaining .where() REPLACES — use & or .filter()).
from(Users.table)
    .where(Users.age.between(26, 45) & Users.name.like('%a%'))
    .orderBy(Users.age.asc()).limit(20).offset(0)
    .map(userMapper.read);

// Joins: innerJoin / leftJoin, FK-driven (onFk:) or explicit (on:), self-joins via aliases.
final mgr = Users.table.aliased('mgr');
from(Users.table)
    .innerJoin(mgr, on: Users.managerId.eqColumn(mgr.col(Users.id)))
    .map((r) => '${r.get(Users.name)} -> ${r.get(mgr.col(Users.name))}');

// Aggregates & grouping: count/sum/avg/min/max, groupBy + having.
from(Posts.table)
    .groupBy([Posts.authorId])
    .having(Posts.views.sum().gt(100))
    .select([Posts.authorId, Posts.views.sum()])
    .map((r) => (r.get(Posts.authorId), r.get(Posts.views.sum())));

// Writes: insert / update / delete, plus RETURNING and upsert (ON CONFLICT).
insertInto(Users.table).value(Users.name.set('Bob'));
update(Users.table).set(Users.age.set(31)).where(Users.id.eq(1));
deleteFrom(Posts.table).where(Posts.views.lt(10));
```

## Codegen (derives)

Annotate a data class over your generated schema; `build_runner` emits the boilerplate into `<file>.g.dart`.
Full guide: **[docs/derives.md](docs/derives.md)**.

```dart
@Queryable(Users.table)   // -> $UserFromRow reader, userMapper, userQuery, findUser(pk)
@Insertable(Users.table)  // -> extension: user.toInsert()  (InsertStatement)
@AsChangeset(Users.table) // -> extension: user.toUpdate()  (UpdateStatement SET clause)
class User {
  final int id;
  final String name;
  final int age;
  final int active;
  final int? managerId;

  @Relation(Users.managerId) // read-side self-join, nested & alias-safe
  final User? manager;

  const User(this.id, this.name, this.age, this.active, {this.managerId, this.manager});
}
```

| Annotation | Generates |
|---|---|
| `@Queryable(table)` | `$XFromRow` reader, `xMapper` (`RowMapper<X>`), `xQuery` getter, bare `findX(pk)` |
| `@Insertable(table)` | `extension XInsert on X { InsertStatement<T> toInsert() }` |
| `@AsChangeset(table)` | `extension XChangeset on X { UpdateStatement<T> toUpdate() }` |
| `@Column(col, {readOnly, writeOnly})` | field → column mapping + read/write direction |
| `@Relation(fk, {depth})` | nested related object via joins, unrolled `depth` levels with path aliases |

## Migrations CLI

Run from a directory with a `diesel.yaml` (or `DATABASE_URL` set). Details: **[docs/migrations.md](docs/migrations.md)**.

| Command | Effect |
|---|---|
| `setup` | Create the migrations dir + database and apply pending migrations. |
| `migration generate <name>` | Scaffold `migrations/<version>_<name>/{up,down}.sql`. |
| `migration run` / `revert` / `redo` | Apply pending / roll back latest / revert+re-apply. |
| `migration list` | Show applied vs pending. |
| `database reset` | Recreate the database from scratch. |
| `print-schema [-o <file>]` | Introspect the DB into a typed Dart schema. |

Applied versions are tracked in `__diesel_schema_migrations` (matching the Rust `diesel` CLI), so on SQLite
you can **share a migrations directory and database with the Rust `diesel` CLI**.

## Backends

The DSL, schema, migrations, and codegen are backend-agnostic; pick a `Connection`:

```dart
final sqlite = SqliteConnection.open('app.db');          // or .memory()
final pg = await PostgresConnection.open(
    host: 'localhost', port: 5432, database: 'app',
    username: 'postgres', password: 'postgres', ssl: false);
```

The CLI selects the backend by URL scheme (`postgres://…` vs a SQLite path). Cross-backend value codecs
mean `int`/`text`/`real`/`bool`/`DateTime` columns behave the same on both. See
[`diesel_sqlite`](packages/diesel_sqlite) and [`diesel_postgres`](packages/diesel_postgres).

## DevTools inspector

`package:diesel/devtools.dart` powers a DevTools **"diesel"** tab: pick the active instance, browse / filter
/ sort tables, view and edit rows, and run raw SQL — for SQLite *and* Postgres.

```dart
import 'package:diesel/devtools.dart';
DieselDevTools.register(conn, name: 'main'); // dev-only; absent from release builds
```

Opening the tab needs a Dart Tooling Daemon, so use the launcher:
`dart run example/tool/inspect.dart`, then enable **diesel** in DevTools' Extensions menu. See
[`packages/diesel/README.md`](packages/diesel/README.md#devtools-inspector).

## diesel-rs alignment

diesel_dart deliberately mirrors diesel-rs concepts (schema-first, migration compatibility, derive parity)
while reading like idiomatic Dart. The feature-by-feature matrix is in
**[docs/diesel-rs-comparison.md](docs/diesel-rs-comparison.md)**.

## Documentation

- [Getting started](docs/getting-started.md) · [Query DSL](docs/query-dsl.md) · [Migrations](docs/migrations.md)
- [Derives (codegen)](docs/derives.md) · [Type mapping](docs/type-mapping.md)
- [diesel-rs comparison](docs/diesel-rs-comparison.md) · [Roadmap](docs/ROADMAP.md)
- [CLAUDE.md](CLAUDE.md) — repo guide for contributors and AI agents

## Roadmap

The alignment roadmap **M1–M5 is complete**: SQLite migration compatibility with the Rust `diesel` CLI,
diesel-style API aliases, SQLite query parity, derive parity, and a full Postgres backend. Remaining /
optional work (MySQL, connection pooling, advanced Postgres types, schema-first) lives in
[docs/ROADMAP.md](docs/ROADMAP.md).

## Contributing

Setup, day-to-day commands, the architecture, how to add a backend / derive / CLI command, and the coding
conventions are all in **[CONTRIBUTING.md](CONTRIBUTING.md)** (with [CLAUDE.md](CLAUDE.md) as the exhaustive
repo guide). The short version:

```sh
dart pub get
dart analyze packages example
cd example && dart run build_runner build && dart run bin/example.dart
```

## License

Not yet specified.
