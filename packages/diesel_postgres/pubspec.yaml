name: diesel_postgres
description: Postgres backend for the Diesel Dart ORM — dialect now; driver-backed Connection + introspection are WIP.
version: 0.0.1

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  diesel: ^0.0.1

dev_dependencies:
  test: ^1.26.3
